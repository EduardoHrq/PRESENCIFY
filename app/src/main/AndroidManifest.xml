<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/presencify"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/presencify_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRESENCIFY"
        tools:targetApi="31">
        <activity
            android:name=".form.Formulario"
            android:exported="false" />
        <activity
            android:name=".home.Home"
            android:exported="false" />
        <activity
            android:name=".login_cadastro.Cadastro"
            android:exported="false" />
        <activity
            android:name=".login_cadastro.Login"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>